@inject NavigationManager NavigationManager
@inject IHostCapabilities Caps

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateToAfterAuth($"Account/Login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", Caps.RequiresFullReloadAfterAuth);
    }
}
